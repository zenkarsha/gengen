.social-share-buttons
  margin-bottom 32px
  padding 16px 0
  display flex
  justify-content center
  flex-direction row
  overflow-x scroll

  +below($tablet)
    justify-content flex-start
    box-shadow inset -10px 0 4px -8px rgba(0,0,0,0.1), inset 10px 0 4px -8px rgba(0,0,0,0.1)
    padding-left 16px
